/* ============================================================
 * RIVIERA JOURNEYS — analytics.js [DEPRECATED]
 *
 * As of 2026-04-27 this file is no longer the analytics entry point.
 *
 * Analytics + cookie consent are unified in /cookie-consent.js, which:
 *   - Implements Google Consent Mode v2 (default: denied)
 *   - Renders the consent strip
 *   - Loads GA4 only after explicit consent
 *   - Replaces the inline GA4 snippet that previously lived in <head>
 *
 * This stub is kept only so that any old cached HTML referencing
 * /analytics.js does not 404. It does nothing.
 *
 * Safe to delete once the previous build is no longer in any cache.
 * ============================================================ */
